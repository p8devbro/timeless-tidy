meta {
  name: login admin
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/admin/login
  body: json
  auth: none
}

body:json {
  {
    "email": "john.doe@example.com",
    "password": "admin123"
  }
}
